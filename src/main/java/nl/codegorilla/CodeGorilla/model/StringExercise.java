package nl.codegorilla.CodeGorilla.model;

public class StringExercise {
    public boolean startsWithTest(String string) {
        // TODO This method should return true when string starts with the word test, where
        // it does not matter if the letters of the word test are in upper case or lower case.
        // Thus "test", "Test1", "tEST456" should all return true.
        return false;
    }

    public boolean endsWithTest(String string) {
        // TODO This method should return true when string ends with the word test, where
        // it does not matter if the letters of the word test are in upper case or lower case.
        // Thus "test", "1Test", "456tEST" should all return true.
        return false;
    }

    public boolean atLeastTwoCapitalLetterA(String string) {
        // TODO This method should return true if the string has the letter A at least twice, where
        // A is uppercase only
        return false;
    }

    public int countLetterA(String string) {
        // TODO This method should return the number of times the letter A occurs in the string, where
        // A can either be a lower case a or upper case A.
        return -1;
    }
}
